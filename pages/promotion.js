import React from 'react'
import Page from 'decorators/Page'

class Promotion extends React.PureComponent {
  static pageName = 'promotion'

  render () {
    return null
  }
}

export default Page(Promotion)
